function convertToFahrenit() {
    var celsuis= parseFloat(prompt("Enter the temperature in degree celsius"));
    var convertToFahrenit= (celsius * 9/5) + 32;
    alert(celsius + "degrees celsius is equal to" + fahrenheit.toFixed(2));
}
convertToFahrenit();